
/*
#LeetCode# Q0031 下一个排列
难度：中

执行下一个变换，将数字重新排列成词法上的下一个较大的数字变换。

如果这样的排列不可能，它必须将其重新排列成可能的最低顺序（即按升序排序）。

替换必须是原位的，并且只使用恒定的额外内存。

下面是一些例子。输入在左边的列中，其对应的输出在右边的列中。

1,2,3 → 1,3,2
3,2,1 → 1,2,3
1,1,5 → 1,5,1
*/
#include "leetcode.h"

namespace q0031
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0031
