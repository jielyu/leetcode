
/*
#剑指Offer# Q41 和为s的整个数组 VS 和为s的连续正数序列
输入一个递增排序的数组和一个数字s，在数组中查找这个两个数，是的他们的和正好是s。
如果有多对满足条件的，输出一对即可。

*/
#include "leetcode.h"

namespace sword_q41
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

/*
两个指针在一头一尾尝试，如果之和太大，就前移尾部的指针；如果和太小则后移头部指针
*/

class Solution {
public:
    void func();
};

/*
找出所有和为s的连续正数序列：
以前以后两个指针构成滑动窗口，如果太大则左边的指针右移一位，如果太小则右边的指针右移一位
*/

} // namespace sword_q41
