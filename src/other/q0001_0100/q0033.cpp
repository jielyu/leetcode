
/*
#LeetCode# Q0033 在旋转排序阵列中搜索
难度：中

假设一个按升序排序的数组在你事先不知道的枢轴上旋转。

例如，[0,1,2,4,5,6,7]可能会变成[4,5,6,7,0,1,2]）。

你会得到一个目标值来搜索。如果在数组中找到目标值，则返回其索引，否则返回-1。

你可以假设数组中不存在重复值。

你的算法的运行时复杂度必须是O(log n)。

Example 1:

Input: nums = [4,5,6,7,0,1,2], target = 0
Output: 4
Example 2:

Input: nums = [4,5,6,7,0,1,2], target = 3
Output: -1
*/
#include "leetcode.h"

namespace q0033
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0033
