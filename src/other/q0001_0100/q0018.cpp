
/*
#LeetCode# Q0018 四数之和
难度：中

给定一个数组nums的n个整数和一个整数目标，nums中是否有元素a，b，c和d，这样a+b+c+d=目标？找出数组中所有唯一的四元组，求出目标的和。

注意：在数组中不能包含重复的元素a、b、c、d。

解集不能包含重复的四倍体。

Example:

Given array nums = [1, 0, -1, 0, -2, 2], and target = 0.

A solution set is:
[
  [-1,  0, 0, 1],
  [-2, -1, 1, 2],
  [-2,  0, 0, 2]
]

*/
#include "leetcode.h"

namespace q0018
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0018
