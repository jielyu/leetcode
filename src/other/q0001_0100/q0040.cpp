
/*
#LeetCode# Q0040 组合总数2
难度：
给定一个候选号（候选号）和目标号（目标号）的集合，在候选号中找到候选号与目标号相加的所有唯一组合。

每个候选号中的每个数字只能在组合中使用一次。

注：注意。

所有的数（包括目标数）都是正整数。
求解集不能包含重复组合。

Example 1:

Input: candidates = [10,1,2,7,6,1,5], target = 8,
A solution set is:
[
  [1, 7],
  [1, 2, 5],
  [2, 6],
  [1, 1, 6]
]
Example 2:

Input: candidates = [2,5,2,1,2], target = 5,
A solution set is:
[
  [1,2,2],
  [5]
]

*/
#include "leetcode.h"

namespace q0040
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0040
