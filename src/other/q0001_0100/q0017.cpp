
/*
#LeetCode# Q0017 电话号码的字母组合
难度：中
给出一个包含2-9以内的数字字符串，返回所有可能的字母组合。

下面给出了一个数字与字母的映射（就像电话按钮一样）。注意，1并不映射到任何字母。
注意：虽然上面的答案是按词法顺序，但你的答案可以按你想的任何顺序。

Example:

Input: "23"
Output: ["ad", "ae", "af", "bd", "be", "bf", "cd", "ce", "cf"].
*/
#include "leetcode.h"

namespace q0017
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0017
