
/*
#LeetCode# Q0067 二进制加
难度：低

给出两个二进制字符串，返回它们的和（也是一个二进制字符串）。

输入的字符串都是非空的，并且只包含1或0字符。
例子1.输入：a="11"，b="1"。

输入：a="11"，b="1"
输出： "100"
例子2.输入：a="1010"，b="1011"。

输入：a="1010"，b="1011"
输出： "10101"
 

限制。

每个字符串只由'0'或'1'字符组成。
1 <= a.length, b.length <= 10^4
每个字符串要么是 "0"，要么不包含任何前导零。
*/
#include "leetcode.h"

namespace q0067
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0067
