
/*
#LeetCode# Q0025 k组中的反向节点
难度：难

给定一个链接列表，每次将链接列表的结点k反转，并返回其修改后的列表。

k是一个正整数，小于或等于链接列表的长度。如果节点的数量不是k的倍数，那么最后遗漏的节点应该保持原样。

举例说明。

给定这个链接列表。1->2->3->4->5

对于k=2，应该返回：2->1->4->4->3->5。

对于k=3，应该返回：3->2->1->4->5

请注意。

只允许常量的额外内存。
不能改变列表的节点中的值，只能改变节点本身。
*/
#include "leetcode.h"

namespace q0025
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0025
