
/*
#LeetCode# Q0020 有效的括号
难度：低
给定一个只包含字符'('、')'、'{'、'}'、'['和']]'的字符串，判断输入字符串是否有效。

一个输入字符串在以下情况下是有效的。

打开的括号必须被同类型的括号关闭。
开放式括号必须以正确的顺序关闭。
注意，空字符串也被认为是有效的。

Example 1:

Input: "()"
Output: true
Example 2:

Input: "()[]{}"
Output: true
Example 3:

Input: "(]"
Output: false
Example 4:

Input: "([)]"
Output: false
Example 5:

Input: "{[]}"
Output: true

*/
#include "leetcode.h"

namespace q0020
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0020
