
/*
#LeetCode# Q0079 单词查找
难度：中
给定2D板和一个单词，查找单词是否存在于网格中。

该单词可以由顺序相邻的单元格的字母构成，其中“相邻”单元格是水平或垂直相邻的单元格。 同一字母单元不得重复使用。

Example:

board =
[
  ['A','B','C','E'],
  ['S','F','C','S'],
  ['A','D','E','E']
]

Given word = "ABCCED", return true.
Given word = "SEE", return true.
Given word = "ABCB", return false.

限制条件：

木板和单词仅由小写和大写英文字母组成。
1 <=板长<= 200
1 <= board [i] .length <= 200
1 <= word.length <= 10 ^ 3
*/
#include "leetcode.h"

namespace q0079
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

class Solution {
public:
    void func();
};

} // namespace q0079
