
/*
#LeetCode# Q0011 装最多水的容齐
难度: 中
给出n个非负整数a1，a2，...，an，其中每个代表坐标(i，ai)上的一个点。找出两条线，与x轴一起构成一个容器，使容器中的水最多。

注意：不能把容器斜着画，且n至少为2。

示例：
Input: [1,8,6,2,5,4,8,3,7]
Output: 49

*/
#include "leetcode.h"

namespace q0011
{

template<typename T>
bool run_testcases() {
    T slt;

    return true;
}

/*
思路：两个指针分别指向头尾，数值较小的一边向另一边逐个移动，直到最终两个指针相遇。
*/

class Solution {
public:
    void func();
};

} // namespace q0011
